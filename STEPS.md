## Files to REPLACE:
- app/page.tsx
- app/login/page.tsx

## Files/Folders to DELETE:
- app/debug/           (entire folder)
- app/api/debug-ping/  (entire folder)

## Then push:
git add .
git commit -m "fix: mobile responsive + session persistence, remove debug"
git push
